export default 'hello there'
export const description = `This library is shared with your firebase functions.

It's bundled by webpack into ~/functions/lib.js. Right now, you run:

  npx webpack --config lib/webpack.config.js

To build it.
`